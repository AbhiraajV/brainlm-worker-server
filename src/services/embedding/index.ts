export { embedText, cosineSimilarity } from './embedding.service';
export type { EmbedTextInput, EmbedTextResult, EmbeddingConfig } from './types';
export { DEFAULT_EMBEDDING_CONFIG } from './types';
